<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.learn_spanish">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"

        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".phrasesActivity" android:label="PHRASES"/>
        <activity android:name=".colorActivity" android:label="COLORS"/>
        <activity android:name=".familyActivity" android:label="FAMILY MEMBERS"/>
        <activity android:name=".numberActivity" android:label="NUMBER"/>
        <activity android:name=".MainActivity"  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>